+alias org.eolang.math.number

+package c2eo.coperators

[a] > as-int32
  (number a).as-int > value!
  if. > @
    (value.as-bytes.slice 4 1).as-int.gte 0
    (00-00-00-00.concat (value.as-bytes.slice 4 4)).as-int
    (FF-FF-FF-FF.concat (value.as-bytes.slice 4 4)).as-int
